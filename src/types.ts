export type SectionId = 'home' | 'content' | 'stream' | 'lab';

export interface InstagramPost {
  url: string;
}

export interface EtsyProduct {
  url: string;
  title: string;
  image: string; // Placeholder for custom product card design
}
